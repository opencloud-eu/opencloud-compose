---
services:
  opencloud:
    environment:
      IDM_ADMIN_PASSWORD_FILE: /run/secrets/idm_admin_password
    secrets:
      - idm_admin_password

secrets:
  idm_admin_password:
    file: ./secrets/idm_admin_password
